[project]
name = "linkedin2username"
version = "0.30"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.34.2",
    "selenium>=4.44.0",
    "urllib3>=2.7.0",
]

[dependency-groups]
dev = [
    "pytest",
]
